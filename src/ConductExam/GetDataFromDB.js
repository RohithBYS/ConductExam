import PropTypes from 'prop-types'
import React, { Component } from 'react'

export default class GetDataFromDB extends Component {
  static propTypes = {second: third}

  render() {
    return (
      <div>GetDataFromDB</div>
    )
  }
}
